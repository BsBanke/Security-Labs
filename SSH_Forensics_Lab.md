Objective:
Configure, analyze, and defend a remote SSH connection between two distinct operating systems within a local network sandbox environment

Environment:
Attacker machine - Windows 11 desktop

defender machine - Linux Mint laptop

Technologies used - OpenSSH, TCP/IP, Wireshark, Bash Shell, Linux system Admin


Phase 1: Exploit & Remote management (offensive view) 
Actions Taken - Initiated a secure remote terminal session from Windows to Linux using OpenSSH
(ssh [user]@IPAddress

Troubleshot authentication and credential mismatches, validating user existence parameters on the target system

Executed remote post-exploitation staging techniques, simulating an unauthorized actor creating directories and text documents.


Key concepts:
Bash history caching. Documenting the behavior of the Bash shells RAM caching.

Phase 2: Live network analysis (Monitoring view)
Actions Taken - configured a live network packet capture using Wireshark on the target interface.

Inpected protocol-level differences between connection failures. Obsereved how invalid accounts cause immediate TCP rst teardowns, whereas legitimate accounts trigger a graceful, encrypted authentication loop within stable TCP sessions

Watched real-time data payload changes, identifying the specific network packet footprints generated by single keystrokes and system commands traversing an encrypted SSH pipeline

Phase 3: Incident Response & Remediation
Actions Taken - Developed an incident containment strategy to sever unauthorized sessions without destroying volatile forensic evidence (RAM)
Audited active processes to map netowrk sockets to specific Process IDs using process management tools
(ps -ef | grep sshd) and (ss -tulpn)

Documented the procedural commands required to forcefully kill and adversary's session loop (kill -9 [PID]), disable the incoming gateway (systemctl stopssh), and modify local firewall rules via UFW (ufw deny 22/tcp) to prevent immediate lateral movement or automated persistence scripts


Summary of technical competencies:
By completing this hands-on simulation, I have demonstrated practical experience in:
1. Network Infrastructure: Understanding routing boundaries, local private IP schemes vs. global routable spaces, and basic port verification

2. Linux ForensicsL Navigating file system metadata (ls -la --time-style), and auditing system logs (/var/log/auth.log), and isolating user interaction contexts

3. Threat Hunting Theory: Analyzing the mechanics of kernel-level hooking (rootkits), event logging, and behavioral baseline tracking.
